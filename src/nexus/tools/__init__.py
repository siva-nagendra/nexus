"""Tool modules — core, deferred, sceneforge, mlserver, and workflows.

Core tools (~15) are always visible.
Deferred tools (~30) are discovered via Tool Search.
SceneForge tools (~18) wrap the NexusSceneForgeHandler C++ commands.
MLServer tools (~10) call the FastAPI sidecar directly via HTTP.
Workflow tools (~15) orchestrate multi-step operations server-side.
"""
